# Session 3: Auth + Landing + Magic Link
**Date**: 2026-04-18
**Model**: Opus 4.7
**Goal**: Build Phase 0 auth scaffolding, v1-matching landing, real magic link auth flow. Prepare for Phase E (Cloudflare deploy).

## Starting state
- Session 2 closed at commit 0b89724
- Vite + React + TS scaffold working
- v1 visual identity extracted into src/styles/
- Placeholder landing showing "CareerKete v2" with v1 tokens
- No auth, no routing, no Supabase wiring

## Phases completed

### Phase A - Supabase client setup
- src/lib/supabase.ts - typed singleton
- src/vite-env.d.ts - typed ImportMetaEnv
- .env.local created (gitignored) with real VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY
- .env.example updated with proper var names
- Types clean, no commit yet

### Phase B - Routing + AuthContext
- React Router v7 with RouterProvider + createBrowserRouter
- src/lib/auth.tsx - AuthContext, AuthProvider, useAuth() hook
- src/components/ProtectedRoute.tsx - redirects to /login if no session
- src/pages/Landing.tsx, Login.tsx, AuthCallback.tsx, AppHome.tsx (placeholders)
- Subscription to supabase.auth.onAuthStateChange with loading state
- User verified: /, /login, /auth/callback all load; /app correctly redirects to /login when not authenticated

### Phase C - Landing page (rewrite)
- Initial attempt: marketing-page design with 2x2 feature grid, stats strip, 1100px wide layout - WRONG
- User correctly called out deviation from brief (v1 match first, new features later)
- Rewrote to match v1 pixel-perfect:
  - 720px .app shell
  - .hdr with .logo + .logo-m + .logo-t (italic terra on Kete)
  - Saved + profile .ib icons top right (route to /login when clicked)
  - .tabs row with Chat active (clicking any tab routes to /login)
  - .vw.active.chat-a with .welc hero: sprout, "Kia ora - let's find your thing" (italic terra on "your"), warm subhead
  - .go CTA "Let's chat ->" routes to /login
- Underline bug found and fixed: React Router <Link> renders real <a>, browser defaults added underline + link color. Fix was 8-line a.logo/a.tab/a.ib/a.go compat block in components.css
- All landing-* classes removed from components.css (155 lines of garbage cleaned up)

### Phase D - Magic link auth flow
- /login: real form with 4 states (INITIAL, SUBMITTING, SENT, ERROR)
- Email input with v1 .ag-pf input styling
- supabase.auth.signInWithOtp with window.location.origin redirect
- Friendly error translation (no raw Supabase errors shown)
- /auth/callback: detectSessionInUrl handles token, spinner while exchanging, redirect to /app on session
- /app: real sign-in confirmation showing user.email + working Sign out button (calls supabase.auth.signOut then navigates to /)
- END-TO-END TESTED SUCCESSFULLY:
  - User entered gazagnzai@gmail.com
  - Real email received from Supabase
  - Clicked magic link -> landed on /auth/callback -> auto-redirected to /app
  - /app showed "You're signed in as gazagnzai@gmail.com"
  - Sign out worked, returned to /login
  - Re-attempting /app after signout correctly redirected to /login (guard working)

## Key technical decisions this session
- Bundled Phases A+B+C into one commit, then Phase D separately (coherent history)
- Used existing v1 CSS classes everywhere - avoided inventing new login-* or landing-* classes
- Magic link uses window.location.origin so works across any port
- Auth state: single AuthContext, loading: true during initial getSession to prevent flash-redirect
- Supabase Site URL: http://localhost:5173
- Supabase Redirect URLs configured: localhost ports 5173-5178, all with /auth/callback path

## Commits made this session
- eefac4a: Phase 0: Auth scaffolding + v1 landing (A+B+C bundled)
- d05b04e: backlog: logo redesign tied to Kete meaning
- 6dcc848: Phase 0: Magic link auth flow (Phase D)

## Current state of codebase
- Dev server running at http://localhost:5177
- Full auth flow working end-to-end
- v1 aesthetic preserved throughout
- No application features yet - just auth shell

## Infrastructure state
- GitHub: https://github.com/ggourliegh/careerkete-schools (public)
- Supabase: careerkete-schools project, Singapore region
- Supabase URL: https://mbbayluwwlonshlcoxwz.supabase.co
- Supabase keys: in .env.local (gitignored), not committed
- Cloudflare: account exists, Pages NOT YET connected
- Domain: NOT YET registered (will use free *.pages.dev initially)

## Backlog items added this session
- Logo redesign tied to Kete (Māori woven basket) meaning - in docs/ROADMAP.md Design Backlog section

## Next session (Session 4) should do
PHASE E - Cloudflare Pages Deploy:
1. Create wrangler.toml or equivalent Cloudflare Pages config
2. User connects GitHub repo to Cloudflare Pages via dashboard
3. Configure build: npm run build, output dir dist/, env vars VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY
4. First deploy to free *.pages.dev URL
5. Update Supabase redirect URLs to include production URL
6. Test full magic link flow on public URL
7. Bundle as commit "Phase 0: Cloudflare Pages deploy + production auth"

## Open questions for Session 4
- Claude API proxy Worker not yet created (deferred to Phase 1 when Chat tab gets built)
- Domain registration deferred until product is mature enough to promote
- Auto-deploy configured? (Cloudflare should auto-deploy on push to main)

## Lessons learned / process notes
- User held me strictly to the brief when I deviated into marketing-page design for landing. That discipline saved time.
- Breaking into Phases A/B/C/D with stops for confirmation worked well - caught the landing deviation before it compounded
- Bash permission prompts are annoying - auto-approving categories (npm, git commit, curl) with option 2 saves significant time
- Dev server port drifted 5173 -> 5174 -> 5175 -> 5176 -> 5177 throughout session due to old tabs. Need to kill old processes or close browser tabs at session start to avoid confusion.

END OF SESSION 3 NOTES
